s = []                          # queue to store traversal
ce = []                         # list of final solution
n = input()                     # number of nodes
a = [0 for i in range(n)]       # list of adjacent nodes
v = []                          # list of vertices
g = [tuple()]                   # list of edges, each edge is a 2-tuple

def euler(v, a[]):
    s.empty()
    ce.empty()
    x = 1
    s.push(x)
    while s.empty() == False:
        x = s.top()
        if a:
            y = a[x]
            s.push(y)
            remove 
        else:
            s.pop()
            ce.push(x)
            
